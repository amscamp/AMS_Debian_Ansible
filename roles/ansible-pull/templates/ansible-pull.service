{{ ansible_managed|comment }}

[Unit]
Description=system configuration upgrade
After=network-online.target
 
[Service]
ExecStart=/bin/sh -c 'until host yourhost.com; do sleep 1; done && ansible-pull -U "{{ ansible_pull_url }}" -i "$(uname -n)," --diff'
TimeoutStopSec=600
