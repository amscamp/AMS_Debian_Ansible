[Unit]
Description=configure plasma
After=default.target

[Install]
WantedBy=default.target

[Service]
Type=simple
ExecStart=/home/{{ desktop_username }}/.config/ansible_distro/configure_plasma_shell.sh