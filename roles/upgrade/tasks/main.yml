---
- name: Upgrade the OS (apt-get dist-upgrade)
  ansible.builtin.apt:
    upgrade: dist
    update_cache: true
- name: install pdfsam
  apt:
    state: latest
    name: "{{ item }}" 
  loop:
    - pdfsam
